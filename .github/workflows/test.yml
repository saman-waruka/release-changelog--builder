name: 'CI Release Changelog Builder'
on:
  push:

jobs:
  echopath:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: PWD
        run: pwd
      - name: list file 
        run: ls 
      - name: Cat file 2  
        run: cat config-release-builder/config.json
      - name: Cat file 
        run: cat ~/config-release-builder/config.json